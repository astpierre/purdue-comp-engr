Build + Setup:
  ./buildme   # Creates class files
  
To run:
  rmiregistry &         # Starts the rmi registerer (in bg)
  java MyRemoteImpl &   # Starts the server hosting the RMI services (in bg)

  java MyRemoteClient   # Results printed to stdout (in fg)

  ./cleanme   # Deletes all files generated by build / setup