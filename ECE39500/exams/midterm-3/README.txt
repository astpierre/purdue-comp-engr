Build + Setup:
  ./buildme   # Creates class files
  ./setup     # Generates RMI stub and starts MyRemoteImpl
  
To run:
  java MyRemoteClient   # Results printed to stdout
  ./cleanme   # Deletes all files generated by build / setup